{
  "name": "od-wordpress-monitor",
  "version": "0.1.0",
  "private": true,
  "description": "Local development tooling for the OD WordPress Monitor plugin.",
  "scripts": {
    "env:start": "wp-env start",
    "env:stop": "wp-env stop",
    "env:status": "wp-env status",
    "env:logs": "wp-env logs",
    "env:cli": "wp-env run cli wp"
  },
  "devDependencies": {
    "@wordpress/env": "^11.14.0"
  }
}
